import { EnginehoursPipe } from './enginehours.pipe';

describe('EnginehoursPipe', () => {
  it('create an instance', () => {
    const pipe = new EnginehoursPipe();
    expect(pipe).toBeTruthy();
  });
});
